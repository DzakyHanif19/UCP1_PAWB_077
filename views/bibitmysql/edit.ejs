<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Edit Data Bibit</title>
  <!-- Bootstrap CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/bootstrap-icons/1.5.0/font/bootstrap-icons.min.css">
  <style>
    body {
      background-color: #f8f9fa;
    }
    .container {
      max-width: 600px;
    }
    .card {
      box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
    }
    .btn-primary {
      background-color: #4CAF50;
      border: none;
      transition: background-color 0.3s ease;
    }
    .btn-primary:hover {
      background-color: #45a049;
    }
    .btn-secondary {
      transition: background-color 0.3s ease;
    }
    .btn-secondary:hover {
      background-color: #6c757d;
    }
  </style>
</head>
<body>
  <div class="container mt-5">
    <h1 class="text-center mb-4 text-primary">Edit Data Bibit</h1>

    <div class="card p-4 bg-white rounded">
      <form action="/bibitmysql/edit/<%= data.id %>" method="POST">
        <div class="mb-3">
          <label for="nama" class="form-label">Nama Bibit:</label>
          <input type="text" id="nama" name="nama" value="<%= data.namabibit %>" class="form-control" required>
        </div>
        
        <div class="mb-3">
          <label for="jenis" class="form-label">Jenis Bibit:</label>
          <input type="text" id="jenis" name="jenis" value="<%= data.jenis %>" class="form-control" required>
        </div>

        <button type="submit" class="btn btn-primary w-100"><i class="bi bi-save"></i> Simpan Perubahan</button>
      </form>

      <div class="mt-4 text-center">
        <a href="/bibitmysql" class="btn btn-secondary"><i class="bi bi-arrow-left-circle"></i> Kembali ke Daftar Bibit</a>
      </div>
    </div>
  </div>

  <!-- Bootstrap JS -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
